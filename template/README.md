# docker4gis-angular

## Getting Started

Before building this component image, scaffold an Angular app inside the
component directory.

Example:

```bash
# from the component directory
ng new ${DOCKER_USER}-app
```
